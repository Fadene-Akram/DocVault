spring.application.name=comments-service
server.port=8083

# PostgreSQL Configuration (disabled for Docker Compose - was used for K8s)
# spring.datasource.url=jdbc:postgresql://comments-postgres-service:5432/commentsdb
# spring.datasource.driverClassName=org.postgresql.Driver
# spring.datasource.username=admin
# spring.datasource.password=password123
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=false

# Cassandra Configuration (ACTIVE)
spring.cassandra.keyspace-name=dms
spring.cassandra.contact-points=${CASSANDRA_HOST:localhost}
spring.cassandra.port=9042
spring.cassandra.local-datacenter=datacenter1
spring.cassandra.schema-action=CREATE_IF_NOT_EXISTS

management.endpoints.web.exposure.include=health
management.endpoint.health.probes.enabled=true

# H2 Configuration (disabled)
# spring.datasource.url=jdbc:h2:mem:commentsdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.h2.console.enabled=true